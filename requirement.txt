# ha-enviro-plus requirements
# Compatible with Raspberry Pi OS (Bookworm/Bullseye) + Python 3.11
# Install pinned but not frozen versions for stability.

# MQTT client library
paho-mqtt>=2.0.0,<3.0.0

# System information utilities
psutil>=5.9,<6.0

# Pimoroni Enviro+ stack
enviroplus>=1.0.0,<2.0.0
ltr559>=0.1.2,<1.0.0
bme280>=0.5.3,<1.0.0